<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
   PUBLIC "-//mybatis.org//DTD Mapper 3.0/EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
   
<mapper namespace="com.mycom.euum.review.mapper.ReviewMapper">

	<insert id="requestInsert">	
		<![CDATA[
			insert into REQUEST_TBL(REQUEST_NUM, REQUEST_CATEGORY, REQUEST_USE, REQUEST_MONEY,
									MEMBER_NUM, REQUEST_WRITER, REQUEST_CONTENT, REQUEST_TITLE)
			values(REQUEST_NUM_SEQ.NEXTVAL, #{requestCategory}, #{requestUse}, #{requestMoney}, #{memberNum},
					#{requestWriter}, #{requestContent}, #{requestTitle})
		]]>
		
	</insert>

</mapper>